# 项目亮点

本文档整理 `qwen_coder` 这套实现里最核心的四个能力：子 agent、上下文压缩与前缀缓存、长期记忆、沙箱隔离。

## 1. 子 agent 构建与新增

这个项目里的子 agent 不是在代码里硬编码出来的类实例，而是通过一份 `.md` 文件动态定义出来的配置对象。它的流程可以按“发现 -> 解析 -> 组装配置 -> 过滤工具 -> 执行 -> 返回”来看。

### 1) 发现

主 agent 在工具分发阶段遇到 `agent` 工具时，会把这次请求交给子 agent 系统处理。

- 父 agent 先把工具调用参数整理好。
- 如果工具名是 `agent`，就进入 `handle_agent_tool()`。
- `handle_agent_tool()` 会根据子 agent 名称去查配置。

这一层的重点是：父 agent 不直接“实现”子 agent，而是负责把请求转交给一个独立运行单元。

### 2) 加载

子 agent 的定义来自 Markdown 文件，文件本身就是配置源。

- 读取顺序有优先级。
- 项目目录 `.claude/agents/` 优先级最高。
- 其次是用户目录 `~/.claude/agents`。
- 最后才是内置 `agents/`。

所以新增子 agent 的方式不是改一大坨代码，而是新增一个 `.md` 定义文件。

这个 `.md` 文件会被拆成两部分：

- frontmatter：存配置字段
- 正文：存 system prompt

换句话说，文件本身就是“子 agent 的声明式定义”。

### 3) 为什么写一个 `.md` 就能新建一个 agent

原因不是 Markdown 本身有魔法，而是代码明确把 `.md` 当成 agent 配置来源。

加载器会：

- 扫描目录里的 `*.md`
- 读取文件内容
- 解析 YAML frontmatter
- 把 frontmatter 转成 `SubagentConfig`
- 把正文转成 `system_prompt`
- 用文件名或 `name` 字段注册成一个可调用的 agent

所以这里的关系是：

- 不是“先有 agent，再去写 md”
- 而是“写了 md，加载器把它变成 agent”

### 4) 配置字段怎么映射到运行时

`SubagentConfig` 是真正承载运行时行为的对象，字段和行为是一一对应的：

- `name`：agent 的唯一标识，也是外部调用时的类型名。
- `description`：告诉上层这个 agent 适合做什么。
- `system_prompt`：子 agent 的行为说明，会被塞进 `SystemMessage`。
- `allowed_tools`：白名单，控制它能看到哪些工具。
- `disallowed_tools`：黑名单，在白名单基础上再剔除。
- `permission_mode`：决定子 agent 的权限模式。
- `model`：指定子 agent 用什么模型，`inherit` 表示沿用父级。
- `max_turns`：限制它最多能在内部循环多少轮。
- `timeout_seconds`：限制它最多运行多久。

默认值也都在代码里定义好了：

- `disallowed_tools` 默认禁掉 `agent`、`enter_plan_mode`、`exit_plan_mode`
- `max_turns` 默认 50
- `timeout_seconds` 默认 900 秒

所以 `.md` 文件里的字段不是“备注”，而是真正参与执行决策的配置。

### 3) 过滤

子 agent 不是天然拥有所有工具，它拿到的工具会被过滤。

- 先看 `allowed-tools`。
- 再看 `disallowed-tools`。
- 再结合当前权限模式做二次裁剪。
- 最终只会保留它能合法使用的工具子集。

这一步很重要，因为它决定了子 agent 的能力边界。

### 5) 执行

真正执行时，子 agent 会在自己的上下文里跑一个小循环。

- 它有独立消息列表。
- 它有自己的 system prompt。
- 它最多跑 `max-turns` 次。
- 它还会受 `timeout-seconds` 限制。
- 如果它调用工具，工具调用结果会被继续喂回它自己的循环里。

这意味着子 agent 不是“调用一次就结束”，而是一个完整但受限的自治循环。

### 6) 返回

子 agent 结束后，结果只会以字符串形式返回给父 agent。

- 不返回完整推理轨迹。
- 不共享完整内部消息。
- 只交付最终文本结果。

### 新增一个子 agent 的完整流程

如果你要新增一个子 agent，实际步骤就是：

1. 在 `.claude/agents/` 下创建一个 Markdown 文件。
2. 在 frontmatter 里写清 `name`、`description`、工具权限、模型、轮数、超时等字段。
3. 在正文里写这个子 agent 的行为说明，也就是它的 system prompt。
4. 如果需要额外能力，再给它挂 `skills`。
5. 保存后，主 agent 就能通过 `agent` 工具调到它。

所以“新增子 agent”本质上是“新增一个可配置的受限执行器”，不是新增一段硬编码逻辑。

## 2. 上下文压缩与前缀缓存

这部分是两条流水线叠在一起：一条处理“消息怎么进入模型”，一条处理“上下文怎么减脂”。

### 1) 先组装请求

每次 `agent_node` 调用模型前，都会先把请求拆成两部分：

- `SystemMessage(content=prompt)`：当前轮的系统提示。
- `history`：本轮对话历史，也就是 `state["messages"]`。

`prompt` 不是单纯的固定字符串，而是动态拼出来的系统提示。它内部还分成两段：

- 稳定前缀：身份定义、工具规则等尽量不变的内容。
- 动态后缀：环境信息、`CLAUDE.md`、权限模式、git 状态、skills 等会变的内容。（相关记忆**不在这里**——已改为 append-only 注入消息历史，见第 3 节）

把稳定内容放前面，是为了给后端的 prefix cache 提供尽可能长的稳定前缀。

### 2) 再决定缓存方式

模型后端不同，缓存方式也不同：

- Anthropic / DashScope 走显式缓存，稳定前缀会被打上 `cache_control: {"type": "ephemeral"}`。
- OpenAI / Qwen 走自动前缀缓存，不显式打标，但要求前缀字节尽量稳定。

实际实现里，代码会在真正调用模型之前决定：

- 如果支持显式缓存，就给系统 prompt 的稳定段和历史的最后一条消息加缓存标记。
- 如果不支持显式缓存，就把完整 prompt 原样送进去，让后端自己做 prefix cache。

这里的原则很简单：

- 稳定前缀放最前面。
- 动态内容放后面。
- 最后一个历史消息只在必要时补一个缓存边界点。

一个更具体的消息格式例子大致是这样：

```python
messages_for_llm = [
    SystemMessage(content=[
        {
            "type": "text",
            "text": "稳定前缀：身份定义 + 工具规则",
            "cache_control": {"type": "ephemeral"},
        },
        {
            "type": "text",
            "text": "动态后缀：cwd / CLAUDE.md / permission / git / skills",
        },
    ]),
    HumanMessage(content="帮我检查一下这个函数"),
    HumanMessage(content="## 相关记忆\n...\n### 记忆：代码风格偏好\n..."),  # 仅命中时存在，append-only
    AIMessage(content="我先看一下文件"),
    ToolMessage(
        content="文件内容......",
        tool_call_id="call_123",
    ),
]
```

这里的关键点是：

- `SystemMessage.content` 不是纯字符串，而是一个 block 列表。
- 只有稳定前缀 block 会加 `cache_control`。
- 动态后缀 block 不加标记。
- 相关记忆不属于 `SystemMessage` 的动态后缀；命中时会作为一条独立 `HumanMessage` 追加到消息历史末尾。
- 历史消息会在构建前做一次缓存边界处理，必要时只给最后一条消息补标记。

### 3) 再处理历史消息：两道压缩在调用 LLM 前依次跑

`agent_node` 在真正调用模型前，会对本轮 history 做两道处理，顺序固定：先 time-based mic，再 LLM snip。

**第一道：Time-based Micro-compact（看时间，不看内容）**

判断距上次 API 调用是否超过 5 分钟。Qwen 的计费缓存 TTL 正好是 5 分钟，超过就意味着缓存几乎必然已经失效——反正下一轮要重写整个前缀，那就趁机把旧的可再生工具结果内容清掉：

- 只清可重算的工具结果（`read_file` / `grep_search` / `list_files` / `run_shell` / `web_fetch`）
- 保留最近 5 条
- 关键点：**只改本轮发给 LLM 的 history 副本，不写回 state**。因为缓存本来就失效了，这次清理是“免费搭车”，没必要持久化

**第二道：LLM Snip（让模型自己判断哪段冗余）**

这道和 cc-haha 的思路一致：不靠 token 阈值机械触发，而是攒够一批消息就问一次模型。具体是用一个计数器记录“上次检查时的消息数”，每当 non-system 消息又多了 20 条，就把整段历史带上序号发给模型，让它回答两件事：

- 需不需要压缩？不需要就返回 `{"needed": false}`，本轮直接跳过
- 需要的话，返回要折叠的序号区间 `[start, end]` 和这段的摘要

然后只把 `[start, end]` 这一段折叠成 1 条摘要消息，区间外的消息原封不动。比如 30 条里模型认为第 5–13 条是冗余的探索过程，就把这 9 条换成一句“读了 X，发现 Y，决定用 Z”，剩下 21 条完全不动。最近 6 条永远不参与压缩，就算模型给了越界序号也会被强制裁回。

实现上用 `RemoveMessage` 把被折叠的旧消息从 state 删掉，再插入 1 条摘要 `HumanMessage`。不论这轮是否真的压缩，都会重置计数器——这样模型说“不用压”的时候，不会每轮都被反复询问。

### 4) 真正到上限时：用户确认的全量摘要

前面两道是后台自动跑的轻量压缩。如果上下文使用率真的冲到 75%，`warn` 节点会用 `interrupt()` 弹出提示问用户要不要压。用户确认且使用率已过 85% 时，进入全量摘要：

- 把**全部历史**交给模型（不保留任何原始消息）
- 模型先用 `<analysis>` 草稿分析，再输出 9 段式 `<summary>`（主要请求 / 技术要点 / 文件代码 / 错误修复 / 问题解决 / 全部用户消息 / 待续事项 / 当前工作 / 下一步），这套结构直接对齐 cc-haha 的 `compactConversation`
- 用 `RemoveMessage` 删掉所有原始消息，只留 1 条摘要

cc-haha 在全量压缩后还会把会话期间读过的文件（最多 5 个）重新读一遍注入回来，本项目暂未实现这一步——压缩后模型如果还要改某个文件，自己 `read_file` 重读即可。

### 5) 最后回到 agent

压缩不会改变主流程的方向，它只是把历史体积压下去以后，继续让 agent 重新组装 prompt 并发起下一次推理。

所以整个链路是：

- 先组装 `SystemMessage + history`
- 再决定缓存标记怎么打
- 调用 LLM 前先跑 time-based mic（看时间）和 LLM snip（看内容冗余）
- 真到上限且用户确认时，才在 compress 节点做全量摘要
- 最后回到 agent 继续跑

### 6) 为什么这样设计

核心是让“改历史”尽量发生在“缓存反正要失效”的时刻，把压缩的代价和缓存重建的代价合并成一次：

- **time-based mic 专挑缓存已过期的时机**（空闲 >5min），这时清理工具结果不额外付缓存重建费
- **LLM snip 攒够 20 条才检查一次**，把多次零散的小压缩批量成一次前缀重建，而不是每轮都改一点、每轮都付一次重建费
- 这正是 Anthropic context editing 里 `clear_at_least` 参数的同款思路：别零敲碎打，攒够一批再清

三件事同时保住：

- 缓存命中率（稳定前缀不动，压缩集中在缓存失效点）
- 历史结构稳定性（snip 只折叠 LLM 判定的冗余区间，其余不动）
- 上下文足够短，对话还能继续

## 3. 长期记忆提取与合并

这部分可以按一条流水线来理解：`采集 -> 落盘 -> 检索（含冲突裁决）-> 整合`。

### 1) 采集

采集发生在会话进行中，由 `auto_save_memory()` 负责。

- 每次正常回答结束后，如果没有工具调用，系统以 `asyncio.create_task` **fire-and-forget** 异步触发，不阻塞主 Agent 循环。
- 输入是本轮 `user_message` 和 `assistant_response`（各截取前 800 字符）。
- 模型判断本轮是否有值得跨会话保留的内容，有则返回一组 `memories`（含 `name / description / type / content`）。
- **写路径不做冲突解决**：采集只管”有价值就存”，不感知磁盘已有什么，不比对新旧——冲突交给检索阶段的读时裁决。

这一阶段的目标：先把有价值的信息提取出来，不丢掉，不阻塞。

### 2) 落盘

模型返回 memory 以后，代码做字段校验，再写入文件系统。

- 只有 `name`、`description`、`type`、`content` 都合法时才继续。
- 调用 `save_memory()`，把 `name` slug 化后拼成 `{type}_{slug}.md`。
- 文件存在则直接**覆盖**（`path.write_text`），不存在则新建。
  - 覆盖是冲突”碰巧被解决”的唯一路径：若两次 LLM 给出相同 `name`，第二次自动覆盖第一次。
  - 但 LLM 两次命名不一致时，两条矛盾记忆会并存于磁盘——这是设计预期，由读时裁决处理。
- 文件的 `mtime` 由操作系统自动更新，成为后续冲突裁决的时间依据。
- 每次写完都刷新 `MEMORY.md` 索引。

### 3) 检索、注入与冲突裁决

每轮主 LLM 调用前，`sideQuery` 从磁盘挑出相关记忆注入消息历史，同时完成冲突的**读时裁决**。

**为什么用读时裁决而非写时解决：**

写时解决需要 `auto_save` 先检索磁盘同类记忆再决定覆盖——这会把 fire-and-forget 变成阻塞调用，且每轮多一次额外 LLM 开销。读时裁决利用已有的 `sideQuery` 调用顺带处理，zero 额外成本，写路径保持轻量。

**冲突裁决全流程（以”先说喜欢 Python，几分钟后改口喜欢 Java”为例）：**

```
步骤一：写入（两次独立 fire-and-forget，互不感知）

  t=0    用户说”我喜欢 Python”
         → auto_save → 写 user_编程语言偏好.md (mtime=t0)

  t=5min 用户说”我喜欢 Java”
         → auto_save → 写 user_java偏好.md (mtime=t1)

  此时磁盘：两条并存，内容矛盾

步骤二：检索（sideQuery，下轮 LLM 调用前）

  manifest 每条带精确相对时间：
    - user_编程语言偏好.md: 用户喜欢 Python [类型: user, 记录于: 7 分钟前]
    - user_java偏好.md:    用户喜欢 Java   [类型: user, 记录于: 2 分钟前]

  sideQuery 提示词要求”描述相近/可能冲突的新旧两条全部选出”
  → 两条都被选中（不会只捞到旧的 Python 那条而漏掉新的 Java）

步骤三：注入（append-only 进消息历史）

  记忆区块开头附裁决指令：
    “若两条记忆就同一事项相互冲突，以记录时间较新的为准；
     若内容互补、并不矛盾，则同时采纳，不因旧而丢弃。”

  ### 记忆：编程语言偏好（user，记录于 7 分钟前）
  用户喜欢 Python

  ### 记忆：Java 偏好（user，记录于 2 分钟前）
  用户喜欢 Java

步骤四：主 LLM 裁决

  看到时间差（7 分钟 vs 2 分钟）
  → 判断为同一主题冲突 → 采信”Java（较新）”

  若两条不矛盾（如”Python 做数据分析”+”Java 做后端”）
  → 两者并存，均采纳，不因一条旧就丢弃
```

**注入细节（append-only，缓存友好）：**

- 选中记忆全文包成**一条 `HumanMessage`**，**追加到消息历史末尾**，紧跟当前用户消息之后，而**不是拼进 system prompt**。
- 写回 state 后位置固定不再变动——除首次召回那一轮外，后续轮次该记忆消息都能被前缀缓存覆盖（与”缓存优化为核心”的主线一致）。
- `surfaced_memories` 记录已注入的文件名，下轮不再重复召回；`_MAX_SESSION_BYTES=60KB` 封顶单会话注入总量。

### 4) 整合（AutoDream）

AutoDream 是冲突解决的**物理清理层**：读时裁决保证当下对话始终用正确值，AutoDream 负责让磁盘最终干净。

触发：**双门槛（≥24h 且 ≥5 会话）** 同时满足，在会话结束时异步运行。

- 读取 `.memory/` 下全部记忆文件的完整内容。
- 让模型输出 `{“delete”: [...], “update”: [...], “create”: [...]}` JSON。
- 按 JSON 执行文件级增删改，`update` 等同于重写文件（不做局部 diff）。
- 状态持久化在 `.memory/.dream_state.json`，互斥锁防并发，完成后重置计数器。

### 四步之间的关系

```
采集（写）           落盘（写）          检索+裁决（读）         整合（写）
──────────          ──────────          ────────────────       ──────────
fire-and-forget     文件级覆盖/新建      sideQuery 全选冲突对    AutoDream
不感知旧记忆         mtime 自动更新       时间戳即时裁决          物理删矛盾条
不解决冲突           slug 相同碰巧覆盖    append-only 注入        磁盘最终收敛
```

这套设计的本质是**读写分离的最终一致性**：

- 写路径极轻（fire-and-forget，不阻塞），代价是矛盾记忆可能短暂并存于磁盘。
- 读路径即时纠偏（每次召回都带时间戳，主 LLM 实时裁决），当下对话始终用正确值。
- AutoDream 兜底做物理清理，保证磁盘长期干净。
- 三层各司其职，没有哪一层既要轻量又要强一致。

## 4. 沙箱隔离

这个项目的沙箱隔离主要体现在命令执行和文件同步层面。它也可以按流程来理解：`初始化 -> 同步 -> 执行 -> 回写 -> 兜底`。

### 1) 初始化

`SandboxManager` 是单例，全会话共享一个 sandbox 实例。

- 第一次需要跑命令时才创建 sandbox。
- 创建后会绑定当前工作区。
- 后续命令都会复用这个实例。

这一层的作用是避免每次都重建隔离环境。

### 2) 同步

执行前，系统会先把本地变更同步到 sandbox。

- 不是全量复制。
- 而是按文件修改时间做增量同步。
- 已知不需要同步的目录和文件类型会被跳过。

这样可以尽量减少同步开销，也避免把无关内容送进沙箱。

### 3) 执行

真正运行命令时，命令是在 sandbox 的 `/workspace` 里执行的。

- 不直接在宿主机工作区跑。
- 命令的副作用优先留在隔离环境。
- 子 agent 里的工具执行也会走这套路径。

因此，正常路径下的文件操作是先在沙箱里发生的。

### 4) 回写

沙箱执行完后，本地工作区并不是完全静止的。

- 本地作为源代码镜像，会在后续同步中吸收变更。
- 下次运行前，再把差异同步回沙箱。

这意味着本地和沙箱之间是一个持续同步的关系，不是一次性拷贝。

### 5) 兜底

如果 sandbox 不可用，系统会提示用户是否回退到本地执行。

- 允许回退时，命令会直接在宿主机上跑。
- 不允许回退时，就保持失败。

所以这套隔离不是绝对安全边界，而是“优先隔离执行，必要时本地兜底”。

## 总结

- 子 agent：通过 `.claude/agents/*.md` 定义，`agent` 工具动态拉起。
- 上下文压缩与缓存：稳定前缀固定化 + 分层压缩历史，兼顾命中率和可用上下文。
- 长期记忆：先自动抽取落盘，再周期性整合合并。
- 沙箱隔离：命令在 sandbox 内执行，本地只做同步和 fallback。
