# 四项目对比：记忆系统（提取 / 注入 / 管理）

> 对比 **qwen-coder（本项目）**、**cc-haha**（Claude Code 同源，TypeScript）、**CodeWhale**（DeepSeek 系，Rust TUI）、**Reasonix**（DeepSeek 系，Go）四套编程 Agent 的长期记忆设计。
>
> **数据来源说明**：qwen-coder / cc-haha / Reasonix 基于**实际源码**核实（Reasonix = `mycode/deepseek-reasonix`）；CodeWhale 仅有分析文档（Rust 源码不在本机），相关结论以文档为准、标注 ⚠️。
>
> ⚠️ **重要更正**：早期分析文档 `reasonix_memory_system.md` 称 Reasonix 用「BM25 + memory 工具（search/read/list）」检索、删除时「归档到 .archive」——**经源码核实，二者均不存在**。Reasonix 实际：索引折进系统前缀，模型用通用 `read_file` 读链接文件，写用 `remember`、删用 `forget`（`os.Remove`，不归档）。本文采用源码事实。

---

## 0. 存储层：高度同源

四者存储层惊人地相似（除 CodeWhale 分层更多），说明这套"Markdown 文件 + 索引"范式已是事实标准：

| 项目 | 存储路径 | 单条记忆 | 索引 |
|---|---|---|---|
| **qwen-coder** | `.memory/`（项目内，随仓库版本控制） | `.md` + frontmatter（name/description/type） | `MEMORY.md`，200 行 / 25KB |
| **cc-haha** | `~/.claude` memdir（AutoMem） | `.md` + frontmatter | `MEMORY.md`，200 行 / 25KB |
| **Reasonix** | `~/.config/reasonix/projects/<slug>/memory/` | `.md` + frontmatter（name/description/metadata.type） | `MEMORY.md`，每条一行 |
| **CodeWhale** ⚠️ | 分 5 层：`~/.codewhale/memory.md`、项目 `.codewhale/anchors.md`、`~/.codewhale/memory/<session>.jsonl` 等 | 视层级而定 | 无统一索引（分层独立） |

qwen-coder 的 `200 行 / 25KB` 索引上限与 cc-haha AutoMem **完全一致**（同源）；Reasonix 也是单文件 + 索引同款范式。

---

## 1. 记忆类型

| 项目 | 类型 |
|---|---|
| **qwen-coder** | 4 类：`user` / `feedback` / `project` / `reference` |
| **Reasonix** | 4 类：`user` / `feedback` / `project` / `reference`（**与 qwen-coder 字面完全相同**）+ 文档记忆（REASONIX.md/AGENTS.md，支持 @import 递归） |
| **cc-haha** | `User` / `Project` / `Local` / `Managed` / `AutoMem`(/`TeamMem`) + CLAUDE.md 层级 |
| **CodeWhale** ⚠️ | 5 层职责分离：用户记忆 / 锚点 / 工作集 / 对话摘要 / 规范状态快照 |

qwen-coder 和 Reasonix 的 4 类完全对齐（user 身份 / feedback 工作指导 / project 进行中工作 / reference 外部指针）——明显同源。

---

## 2. 提取（写入）：谁决定、何时、自动还是手动

这是四者**最大的分歧点**：

| 项目 | 谁决定写 | 何时 | 自动/手动 | 机制 |
|---|---|---|---|---|
| **qwen-coder** | **独立后台 LLM** | 每轮最终回复后 | **全自动** | `auto_save_memory` fire-and-forget：小 LLM 判断本轮有无值得长存的内容，有则写文件 + 更新索引 |
| **cc-haha** | **主模型自己** | 主模型决定 | 模型驱动 | 模型用 Write/Edit 两步写（文件 + `MEMORY.md` 指针），prompt 指示；无独立 LLM |
| **Reasonix** | **主模型 + 用户** | 模型调工具时 / 用户编辑时 | 模型驱动 + 手动 | 模型调 `remember` 写、`forget` 删；用户可手动编辑文档记忆、或用 "#" 快速添加 |
| **CodeWhale** ⚠️ | **分层不同** | 见下 | **半自动** | 用户记忆/锚点=**完全手动**；工作集/摘要/规范状态=自动 |

**两种哲学的对立：**

- **qwen-coder = "带外全自动"**：专门的后台 LLM 替模型判断该不该存。优点是不依赖主模型自觉；代价是每轮一次额外 LLM 调用，且"自动提取"有记错风险（把"这次"当"永远"）。
- **CodeWhale = "可靠性 > 便利性"**：故意让用户记忆/锚点**完全手动**，理由是"**LLM 分不清'这次'和'永远'、'这个项目'和'所有项目'**——记错的成本远高于记不住"。只有客观来源（路径、工具输出）才自动。
- **cc-haha / Reasonix = "模型驱动"**：把"写记忆"做成模型能调的工具，由主模型在对话中自行决定，介于全自动和全手动之间。

> 这是 qwen-coder 最该反思的一点：它是四者里**唯一的"后台 LLM 全自动提取"**，恰恰是 CodeWhale 明确反对的设计。好处是省心，但需警惕"自动记错"。可考虑：auto_save 只在出现明确信号（"记住"/"我习惯用"/"项目决定用"）时触发，降低误存。

---

## 3. 注入位置：索引 vs 全文，前缀 vs 历史 vs 尾部

| 项目 | 索引是否进系统前缀 | 注入的是什么 | 注入到哪 | 缓存影响 |
|---|---|---|---|---|
| **qwen-coder** | ❌ 不保留索引 | sideQuery 选中记忆的**全文** | **消息历史**（append-only HumanMessage） | 老记忆位置固定可缓存；新召回那轮全价 |
| **cc-haha** | ✅ `MEMORY.md` 索引常驻 system prompt | grep 出的内容 | tool_result（模型自取） | 索引稳定可缓存；**写记忆那轮破前缀** |
| **Reasonix** | ✅ **文档全文 + 索引**启动时 `Block()` 折进前缀 | 变更 delta | **尾部** `<memory-update>` 注入下一条 user 消息头 | 前缀运行时永不变；变更轮多 ~50 token；重启折回前缀 |
| **CodeWhale** ⚠️ | 视层级：用户记忆/锚点/规范状态进系统提示不同位置 | 各层内容 | 用户记忆=前缀顶端；锚点/摘要/规范状态=压缩摘要块；工作集=系统提示末端 | 工作集 `summary_block()` 字节级稳定；各块独立失效 |

**核心差异：**
- **谁能看到完整目录**：cc-haha / Reasonix / CodeWhale 都把索引（或用户记忆/锚点）放进**主模型可见的前缀**，模型自己知道"有哪些记忆"；**qwen-coder 不放**——主模型看不到完整目录，"有哪些记忆"这件事外包给了 sideQuery 小模型。
- **注入全文还是索引**：qwen-coder 注入**选中记忆全文**（重）；Reasonix/cc-haha 前缀只放**索引**，全文按需取（轻）。Reasonix 文档：50 条记忆时，索引 ~1500 token vs 全文 ~7500 token。
- **写记忆的缓存代价**：Reasonix 最优——变更走尾部 delta，**系统前缀字节不变**；cc-haha 写记忆会改 `MEMORY.md` → 下轮前缀失效；qwen-coder 因索引不在前缀，写入本身不破前缀（但 auto_save 是异步的，且召回时注入全文）。

---

## 4. 召回（读取）机制

| 项目 | 召回方式 | 额外成本 |
|---|---|---|
| **qwen-coder** | **独立小 LLM**（sideQuery）从索引 manifest 选 Top-5，自动注入历史 | 每轮 1 次小 LLM 调用（即使返回空） |
| **cc-haha** | 索引常驻前缀，模型需要时**自己 grep/read** 记忆目录 | 主模型多一轮往返（仅需要时；占主循环） |
| **Reasonix** | 索引/文档常驻前缀，模型需要细节时**用 `read_file` 读链接文件** | 主模型多一轮往返（仅需要时；占主循环） |
| **CodeWhale** ⚠️ | 各层在固定时机注入（用户记忆启动注入、锚点压缩时注入…），模型直接看到 | 注入是确定性的，无检索调用 |

**重要更正**：早先以为 Reasonix 用 BM25 本地检索（零 LLM 成本），**实际不是**——它和 cc-haha 一样是"索引在前缀 + 模型按需 read_file/grep"的**模型驱动召回**。所以 **Reasonix 与 cc-haha 的召回成本结构其实相同**（都靠主模型多一轮往返），区别主要在写入路径（见 §3）。

只有 **qwen-coder 用独立小 LLM 做预选召回**——这是它的独特处：召回不依赖主模型自觉（强制每轮检索），但每轮固定一次小调用。

---

## 5. 管理 / 整合

| 项目 | 整合机制 | 触发 |
|---|---|---|
| **qwen-coder** | **AutoDream**：LLM 读全部记忆，输出 delete/update/create，合并重复、删过时、更新陈旧 | 双门槛：距上次 ≥24h **且** 累计 ≥5 会话；会话结束时 |
| **cc-haha** | **/dream**（KAIROS）：平时 append-only 写日期日志，**夜间**蒸馏成 `MEMORY.md` + 主题文件 | 夜间定时 |
| **Reasonix** | 保存时 `reindex` 更新索引行（description 是快照）；冲突靠**近因效应**（尾部 delta 覆盖前缀旧索引） | 每次 save；删除 = `os.Remove`（不归档） |
| **CodeWhale** ⚠️ | 无显式整合；锚点压缩时重读、规范状态 JSONL 只追加 | — |

qwen-coder 的 AutoDream 与 cc-haha 的 /dream **思路一致**（定期 LLM 整合），只是触发不同（会话结束双门槛 vs 夜间）。Reasonix 不做主动整合，靠"保存即覆盖 + 近因覆盖"维持最终一致性。

---

## 6. 一句话定位

| 项目 | 风格 |
|---|---|
| **qwen-coder** | **带外全自动**：后台小 LLM 提取 + sideQuery 预选召回 + AutoDream 整合。召回可靠不靠模型自觉，代价是每轮额外调用 + 注入全文 + 自动提取记错风险 |
| **cc-haha** | **模型 in-loop**：索引常驻前缀，模型自己 Write/grep；省调用但占主循环、靠模型自觉 |
| **Reasonix** | **缓存优先 + 模型驱动**：索引/文档折进确定性前缀，写用 remember/forget、变更走尾部 delta（前缀永不变），读用 read_file |
| **CodeWhale** ⚠️ | **可靠性优先 + 分层半自动**：偏好/锚点完全手动（防记错），客观信息（路径/状态）才自动；5 层各司其职 |

---

## 7. qwen-coder 的反思与可借鉴点

1. **警惕"自动提取记错"**（CodeWhale 的核心论点）：qwen-coder 是四者里唯一的后台 LLM 全自动写入，正是 CodeWhale 刻意规避的。建议 auto_save 加信号 gate（仅"记住/我习惯/项目决定"等明确信号才触发），或对自动存的记忆标注来源、便于用户回看纠错。
2. **让主模型看到索引**（cc-haha / Reasonix）：现在主模型看不到完整目录，"有哪些记忆"全靠 sideQuery 小模型代选。可考虑把精简索引也放进前缀，让主模型自己也能判断要不要 read。
3. **索引进前缀 + 内容按需取**：现在注入的是选中记忆全文（重）。若改成"索引常驻 + 模型按需 read_file"，能省 token（Reasonix 数据：50 条记忆 ~1500 vs ~7500）。但这会让召回从"强制预选"变成"靠模型自觉"，是可靠性与成本的权衡。
4. **写记忆走 delta 而非重写索引**（Reasonix）：若未来把索引放进前缀，更新时可学 Reasonix 的尾部 `<memory-update>` 注入，避免每次写记忆都破前缀缓存。
5. **降低每轮召回的固定成本**：sideQuery 每轮一次小调用，可用免费启发式先 gate（纯工具轮、无指代过去信号的轮次跳过），把"每轮必调"降成"按需调"。

---

*生成基于：本项目源码、cc-haha 源码、`deepseek-reasonix` 源码核实，以及 `CodeWhale记忆系统深度分析.md`（CodeWhale 源码不在本机，以文档为准）。*
