# 四项目对比：上下文压缩与前缀缓存策略

> 对比 **qwen-coder（本项目）**、**cc-haha**（Claude Code 同源，TypeScript）、**CodeWhale**（DeepSeek 系，Rust TUI）、**Reasonix**（DeepSeek 系，Go）四套编程 Agent 在「上下文压缩」与「Prompt 前缀缓存」上的设计取舍。
>
> 一句话先行：四者共享同一条物理事实——**改写历史消息 = 前缀缓存失效**，没有任何 provider 提供"原地改缓存"的 API。区别只在于**谁来管这笔账、何时付、付多少**。

---

## 0. 共识：压缩与缓存的根本矛盾

前缀缓存按"从第一个 token 起的连续前缀哈希"索引，是只读 KV。一旦改写靠前的内容，从改动点起全部 miss。因此四个项目都遵循两条共同原则：

1. **稳定内容尽量靠前，易变内容尽量靠后**（让可缓存前缀尽可能长）。
2. **压缩时优先动尾部、保前缀**（裁剪/摘要从新到旧，保护最旧的已缓存前缀）。

差异从这里开始分化。

---

## 1. 前缀结构 / 系统提示布局

| 项目 | 稳定前缀放什么 | 易变部分放哪 | 缓存标记 |
|---|---|---|---|
| **qwen-coder** | `_IDENTITY` + `_TOOL_RULES`（两个硬编码常量） | 动态后缀：env / CLAUDE.md / 权限 / git / skills（记忆已移出，改 append-only 进历史） | Anthropic：稳定前缀打 1 个 ephemeral + 末条消息 1 个，共 2 个；Qwen：靠自动前缀缓存 |
| **cc-haha** | system prompt 分 section 缓存（identity/tools 等） | 记忆索引、env 等按 section | 对齐 Claude Code：system 稳定段 + 末条消息，共 2 个 marker |
| **CodeWhale** | 拆成多个独立 `SystemBlock`：身份 → 工具定义 → 摘要块 → 用户自定义 | 摘要块（压缩时才变）排在靠后 | 块级拆分，摘要变只失效摘要块 |
| **Reasonix** | base system + **记忆索引 + 文档**（启动时 `Block()` 纯函数折叠）+ 标准化排序的工具 schema | 运行时变更走消息尾部，不碰前缀 | Anthropic 仅用 2 个断点（上限 4）：system 末 + 末条消息末 |

**关键差异**：
- **记忆是否进前缀**：CodeWhale/Reasonix 把记忆（索引/用户记忆）**折进稳定前缀**；qwen-coder 现在**不放前缀**（append-only 进历史）；cc-haha 索引常驻 system。
- **工具 schema 排序**：Reasonix 显式按名称排序工具，保证"相同工具集 → 相同 schema 字节"，避免加载顺序导致的无谓失效——这点其他三者未强调。
- **确定性渲染**：Reasonix 的 `Block()` 是纯函数（无随机/时间戳），无记忆时返回空串使 base 字节级不变；这是 CodeWhale「`summary_block()` 字节级稳定」的同款思想。

---

## 2. 消息可变性（最根本的缓存决策）

| 项目 | 历史消息策略 |
|---|---|
| **CodeWhale** | **三条铁律**：① 消息只追加永不原地改；② 工具结果保留原始字节（不美化/不重排 JSON/不规范化换行）；③ thinking 块精确重放。号称带来 ~80% 缓存命中基础 |
| **Reasonix** | append-only 默认，重写是例外且有阈值保护；推理内容只本地 round-trip 不发送 |
| **cc-haha** | 客户端可编辑消息数组，但通过批量化、cache-editing 控制失效代价 |
| **qwen-coder** | 平时 append-only；压缩（snip/全量摘要）时用 `RemoveMessage` 改写，接受一次失效 |

CodeWhale 把"字节不变"提到铁律级别（连 JSON 字段顺序、空格数都不动），是四者里对 append-only 最严苛的。

---

## 3. 工具结果裁剪（免费层 —— 不调 LLM）

所有项目都有"先丢可再生的工具结果"这一最轻量层，但触发与策略不同：

| 项目 | 触发 | 策略 | 缓存友好做法 |
|---|---|---|---|
| **qwen-coder** | ①插入时：单结果 > 10000 字符；②空闲 > 5min（Qwen 缓存 TTL） | ①头 2500 + 尾 500 截断；②清空旧可压缩工具结果内容、保留最近 5 | time-based 专挑缓存已凉时清，不额外付费；只改本轮 history 副本 |
| **cc-haha** | time-based：空闲 > 60min；cached：活跃工具结果**个数**超阈值 | time-based 清内容留最近 N；cached 用 **cache-editing API 服务端删**，本地不动 | cached MC 不破坏前缀（服务端删 + `pinnedEdits` 复发） |
| **CodeWhale** | ①插入时无条件；②压缩时二次裁剪 | ①按工具类型分策略（子代理/测试→结构化摘要；嘈杂→软截断头尾；普通→硬截断）+ metadata 摘要；②**从新到旧**裁剪、重复工具调用只留最新、够了立即停 | 反向裁剪 + 保护窗口（4）+ 增量停止：90% 情况只裁 1-2 条，前 95% 历史不动 |
| **Reasonix** | 80% 阈值 | 只改 >1KB 的 tool 消息 content 为占位符，**不删消息**（不破坏 tool_call/result 配对） | 零 API 调用；触发一次 `LogRewriteVersion` 递增但比全量轻 |

**最精妙的是 CodeWhale 的"从新到旧 + 增量停止"**：因为前缀缓存是从头连续的，从尾部裁、够了就停，能让最旧的已缓存前缀几乎不受影响。Reasonix「不删消息只改 content」、qwen-coder「只改本轮副本」都是同一保护配对/保护前缀的思路。

---

## 4. LLM 摘要压缩（付费层）

| 项目 | 触发 | 保留什么 | 缓存对齐？ |
|---|---|---|---|
| **qwen-coder** | 使用率 > 75% interrupt 问用户，> 85% + 确认 | 全替换为 1 条摘要（不保留原文）；另有 LLM snip 折叠局部冗余区间 | 否——接受一次失效 |
| **cc-haha** | 达阈值（有效窗口 − 13K）自动 / `/compact` | 9 段式摘要全替换，重注入文件/plan/skill/tools；partial 可保留一半 | **是**——forked-agent 共享主线程 prompt cache 前缀；另有 session-memory 复用后台笔记免一次总结 |
| **CodeWhale** | unpinned token > 阈值 且 ≥ 6 条消息 | LLM 摘要未固定消息 | **是——缓存对齐摘要**：原样重放待摘要消息 + 末尾追加一条摘要指令，不重排格式 → 待摘要 N 条 100% 命中，省 70-90% 摘要成本（需 ctx≥500K 等条件） |
| **Reasonix** | 80% 不够 → 摘要中间区域；90% 强制 | 区域三分：`[固定前缀][可压缩区][尾部 16384 tokens]`；可压缩区内**小用户消息 + 旧摘要原文保留**，只摘要 thinking/工具调用/结果；区域 < 400 tokens 跳过 | 固定前缀 + 旧摘要不二次摘要 |

**关键差异**：
- **cc-haha 和 CodeWhale 都做了"缓存对齐摘要"**——让摘要请求本身也命中缓存（cc-haha 靠 forked-agent 共享前缀，CodeWhale 靠原样重放 + 末尾追加指令）。这是省"摘要这次调用"成本的高级手段，qwen-coder 和 Reasonix 未做。
- **保留粒度**：Reasonix 最精细（按消息类型分区保留小用户消息/旧摘要 + 固定 16384 token 尾部）；CodeWhale 保护窗口；cc-haha 有 partial 半段保留；qwen-coder 全量摘要不保留原文（但有 LLM snip 做局部折叠）。
- **经济性检查**：Reasonix（区域 <400 tokens 跳过）和 CodeWhale（≥6 条才摘要）都有"不值得就不压"的 ROI 闸；cc-haha 有连续失败 3 次熔断；qwen-coder 暂无显式 ROI 闸。

---

## 5. 压缩—缓存矛盾的处理总览

| 项目 | 核心策略 | 谁付这笔账 |
|---|---|---|
| **qwen-coder** | 平时稳定前缀省钱；压缩时挑缓存已凉时机（time-based mic）+ 批量化（snip 攒 20 条）；全量摘要接受一次失效 | 客户端，被动接受失效 |
| **cc-haha** | 客户端编辑 + 批量化；cached MC 用服务端 cache-editing 不破前缀；compact 用 forked-agent 共享缓存 | 客户端 + 服务端（cache-editing API） |
| **CodeWhale** | 容量干预决策矩阵（low 不动 / medium 增量裁剪 <5% / high 缓存对齐摘要 ~10% / severe 硬重置 100%），永远先用对缓存影响最小的手段 | 客户端，按风险分级最小化 |
| **Reasonix** | 阈值链渐进降级：50% 警告不动 → 80% 免费裁剪 → 还不够才付费摘要 → 90% 强制；本轮付小成本换跨会话零成本 | 客户端，分层 ROI |

**三种"管账人"**：
- **服务端编辑**：cc-haha 的 cached microcompact / Anthropic context editing（`clear_at_least` 攒够再清）。
- **客户端自律 + 分层降级**：CodeWhale（决策矩阵）、Reasonix（阈值链）最系统化。
- **客户端 + 用户介入**：qwen-coder 独有——> 75% 时 `interrupt()` 让用户决定是否压缩。

---

## 6. Provider 缓存机制差异

| 项目 | 主要后端 | 缓存机制 |
|---|---|---|
| **qwen-coder** | Qwen（OpenAI 兼容）/ Anthropic | Qwen 自动前缀缓存（TTL 5min，命中 ~20%/显式 ~10%）；Anthropic 显式 ephemeral（TTL 5min，1/10 价） |
| **cc-haha** | Anthropic 为主 | 显式 ephemeral + 服务端 cache-editing + 缓存断裂检测 |
| **CodeWhale** | DeepSeek | DeepSeek 自动前缀缓存（从首 token 连续前缀） |
| **Reasonix** | DeepSeek / Anthropic | DeepSeek 自动 + Anthropic 2 断点；`PrefixShape`/`CacheDiagnostics` 可观测，明确告知为何失效 |

Reasonix 的**可观测性**最突出：`PrefixShape`（SystemHash/ToolsHash/PrefixHash/LogRewriteVersion）+ 会话级累计命中统计 + "为什么失效"诊断，让缓存成本透明。

---

## 7. 一句话定位

| 项目 | 风格 |
|---|---|
| **qwen-coder** | 比例阈值 + **用户可介入** + LLM 自主折叠冗余区间（snip）；轻量、关键处问用户 |
| **cc-haha** | 全自动 + 强工程：forked-agent 缓存共享、服务端 cache-editing、后台 session-memory、PTL 重试与熔断 |
| **CodeWhale** | append-only 铁律 + 缓存对齐摘要 + **容量干预决策矩阵**（按风险选最小代价手段） |
| **Reasonix** | **从底层数据结构就以缓存为核心**：确定性前缀折叠 + 阈值链渐进降级 + 强可观测 |

## 8. qwen-coder 可借鉴点

1. **缓存对齐摘要**（cc-haha / CodeWhale）：让"全量摘要这次调用"本身也命中缓存——原样重放待摘要消息 + 末尾追加摘要指令，而非把历史重新格式化成一段文本。当前 qwen-coder 全量摘要会重写格式，摘要请求 0% 命中。
2. **从新到旧 + 增量停止裁剪**（CodeWhale）：time-based mic 目前一次清空所有旧结果（留最近 5）；可改为反向裁剪、释放够了就停，少动几条就少破一点缓存。
3. **ROI 经济性闸**（Reasonix / CodeWhale）：压缩前估算"这次能省多少"，太小就跳过，避免零敲碎打。
4. **工具 schema 排序标准化**（Reasonix）：确保相同工具集生成相同 schema 字节，杜绝加载顺序导致的无谓失效。
5. **缓存可观测**（Reasonix）：记录 PrefixHash / 命中率 / 失效原因，便于验证缓存优化是否真的生效。

---

*生成基于：本项目源码 + `CodeWhale缓存优化深度分析.md` + `reasonix_cache_optimization.md` + cc-haha 源码分析。*
